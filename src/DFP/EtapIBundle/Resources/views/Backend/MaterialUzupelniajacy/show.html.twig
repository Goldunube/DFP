{% extends '@DFPEtapI/Backend/index.html.twig' %}

{% block content -%}
    <h1>Material uzupelniajacy</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ entity.nazwa }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ entity.opis }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('url_materialuzupelniajacy') }}">
            Powrót do listy
        </a>
    </li>
    <li>
        <a href="{{ path('url_materialuzupelniajacy_edit', { 'id': entity.id }) }}">
            Edycja
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
