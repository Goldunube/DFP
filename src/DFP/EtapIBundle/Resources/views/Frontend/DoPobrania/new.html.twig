{% extends '@DFPEtapI/Frontend/index.html.twig' %}

{% form_theme form '@DFPEtapI/Form/bootstrap_form.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet"/>
    <link href="{{ asset('css/bootstrap-theme.min.css') }}" rel="stylesheet"/>
{% endblock stylesheets %}

{% block javascript %}
    {{ parent() }}
    <script type="text/javascript" src="{{ asset('js/bootstrap.min.js') }}"></script>
{% endblock javascript %}

{% block title %}Portal DFP - do pobrania | nowy{% endblock %}

{% block content -%}
    <style type="text/css">
        .container-fluid input[type="file"] {
            height: auto;
            background: none;
            border: none;
        }
    </style>
    <div class="container-fluid">
        <h1 class="page-header">Do pobrania <small>dodaj nowy materiał</small></h1>

        {{ form_start(form) }}
            {{ form_row(form.title) }}
            {{ form_row(form.content) }}
            <div class="form-group">
                {{ form_label(form.publishedDate) }}
                <div class="col-sm-9">
                    {{ form_widget(form.publishedDate) }}
                </div>
            </div>
        <div class="form-group">
            {{ form_label(form.allowedGroups) }}
            <div class="col-sm-9">
                {{ form_widget(form.allowedGroups) }}
            </div>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}